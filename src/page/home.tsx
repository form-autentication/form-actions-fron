import Form from "../components/form/form";

const Home = () => {
   return (
      <div className="home">
         <div className="container">
            <Form />
         </div>
      </div>
   );
};

export default Home;
